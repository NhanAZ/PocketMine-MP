# Roadmap

This is the live state and work queue for NhanAZ's personal PocketMine-MP fork.
Update it during every completed maintenance unit; do not create separate task-context Markdown files.

## Current State

- Fork governance, contribution, security, and production-risk warnings are established.
- Fork-specific changelog tracking is established in `changelogs/fork.md` and backfilled from `a170743de838581487bcf91bc7026fe786de752b` through `6c78bb0b0`.
- `pocketmine/bedrock-block-upgrade-schema`, `pocketmine/bedrock-data`, `pocketmine/bedrock-item-upgrade-schema`, `pocketmine/bedrock-protocol`, `pocketmine/binaryutils`, `pocketmine/callback-validator`, `pocketmine/color`, `pocketmine/errorhandler`, `pocketmine/log`, `pocketmine/math`, `pocketmine/nbt`, `pocketmine/raklib`, `pocketmine/raklib-ipc`, and `pocketmine/snooze` are local Composer path packages.
- Source monitoring covers the PMMP root, PMMP-owned dependencies, and four protocol reference projects.
- The current root matches `upstream/stable` at `fe9f8bd801530ee23ac8e6fb9d8a1922846d5aff`.
- The source audit has 19 entries and eight reviewed package-drift signals.
- PowerNukkitX advanced through `0d0a3b4f9362f4188d3e2b65df47bc93b4f41975` on 2026-07-03; its ItemFilledMap image-loading fix is reference-only evidence for future map-image triage. Earlier RakNet pacing/cookie settings corroborated the RakLib cookie follow-up, and the level-thread/entity tick fix remains reference-only evidence for future cross-level ticking triage.
- RakLib anti-spoofing cookies from canonical commit `7655018631147f0b5d473326fddc2faea105dffe` were adapted behind the opt-in `network.raklib-cookie-rotation-interval` config, which defaults to `0` to preserve OVH-sensitive compatibility.
- The upstream backlog snapshot contains 450 open items and a scored top-40 shortlist.
- Upstream intake progress: 31 of 419 issues and 1 of 31 pull requests have been reviewed as primary candidates; 22 issue fixes were implemented, one issue was already covered, eight issues and one pull request were deferred or rejected, and eight of the current top-40 shortlist remain unreviewed.
- Upstream issue #6284 was triaged and implemented: spawnable tile network NBT now receives a `TypeConverter` context and converter-scoped serialized cache.
- Upstream issue #6711 was triaged and implemented: inventories now reject plain `Item` instances using block-item type IDs before they can crash later during serialization.
- Upstream issue #6580 was reviewed and deferred: the real LevelDB compaction fix requires region-sharded DBs or a world-format migration, not a small safe patch.
- Upstream issue #6808 was triaged and implemented: core entities now save with Bedrock entity IDs while retaining legacy Java/PM aliases for loading older worlds.
- Upstream issue #5632 was triaged and implemented: missing Bedrock blockstate properties now fall back to the registered default state for that block ID while still rejecting wrong property types.
- Upstream issue #6832 was triaged and implemented: chunk loading now skips invalid loaded tiles before block-state sync when their saved position is outside world bounds, duplicated, or in an unavailable chunk.
- Upstream issue #6861 was triaged and implemented: invalid firework rocket `Flight` values loaded from saved NBT now fail as `SavedDataLoadingException` instead of escaping safe item loading as `InvalidArgumentException`.
- Upstream issue #6130 was triaged and implemented: dedicated Query socket startup failures now use `NetworkInterfaceStartException` and receive the server's localized, controlled network-start failure path instead of producing a crash dump.
- Upstream issue #5342 was triaged and implemented by adapting canonical commit `c4fb8832fe99e042801dca60f124b7938c94036f`: translated messages can now restore a base format around formatted parameters, and `/give` contains custom item-name formatting for both the sender and operator audit output.
- Upstream issue #4830 was triaged and implemented: generic entities now invoke block landing behaviour, while slime bounce is limited to living entities and dropped items without adding landing sounds to non-living entities.
- Upstream issue #5603 was triaged and implemented: nether-brick fences no longer connect their collision shape to wooden fences, while all wooden fence variants still connect to each other.
- Upstream issue #5463 was triaged and implemented: update-channel suggestions now use the declared build channel instead of conflating released prereleases with development builds.
- Upstream issue #7035 was reviewed and deferred: world NetworkVersion `944` exceeds the intentionally separate supported-world ceiling `924`, and no affected world or matching upgrade-schema evidence was provided to validate a safe loader change.
- Upstream issue #6712 was triaged and implemented: invalid saved hunger values now fail as `SavedDataLoadingException` instead of escaping strict hunger ranges as `InvalidArgumentException`.
- Upstream issue #6714 was triaged and implemented: projectile hit processing now stops immediately if an event handler or hit hook closes the projectile.
- Upstream issue #5385 was reviewed and deferred: fixing death-screen rejoin correctly requires moving respawn state and `PlayerRespawnEvent` into the login/pre-`StartGamePacket` path, which is too risky for a small fork patch without an explicit plugin-compatibility plan.
- Upstream issue #5638 was triaged and implemented: `PlayerAuthInputPacket` handling now has timing breakdowns for input flags, movement, item-use transactions, item-stack requests, and block actions.
- Upstream issue #5064 was triaged and implemented: `settings.query-player-list` now controls whether GS4 long-query responses include player names while preserving player counts.
- Upstream issue #2626 was triaged and implemented: creative `PREDICT_DESTROY_BLOCK` now synthesizes the missing left-click block interaction only when the block has not already received a start/continue destroy action.
- Upstream issue #6781 was reviewed and deferred: reducing cycle-GC scans for large long-lived arrays needs a PHP/runtime or collection-design strategy, benchmarks, and memory-safety review rather than a small fork patch.
- Upstream issue #4206 was triaged and implemented: replacement chunks passed to `World::setChunk()` now rebase their existing tile coordinates to the target chunk before saving or tile-conflict checks.
- Upstream issue #6800 was triaged and implemented: common inventory search loops now cache the search item's prepared NBT for matching, avoiding repeated search-item NBT rebuilds while preserving existing matching semantics.
- Upstream issue #6818 was triaged and implemented: `date.timezone=UTC` now falls through to system timezone auto-detection because PHP 8.2+ also uses `UTC` as the unset/invalid fallback; users who intentionally want UTC can set `Etc/UTC`.
- Upstream issue #3272 was triaged and implemented: async light population now holds an advisory chunk lock and temporary loader, then discards stale light results if main-thread terrain changes break the lock before completion.
- Upstream issue #3974 was triaged and implemented: chunk population now has a server-wide task slot limiter so many worlds cannot each consume the full configured population concurrency at once.
- Upstream issue #5410 was reviewed and deferred: `FlatWorldLayers` is confirmed as JSON stored in a `level.dat` string tag, but fixing terrain height safely also requires an explicit imported-world Y-offset rule and round-trip evidence for pre-1.18, post-1.18, and PMMP-created flat worlds.
- Upstream issue #2549 was reviewed as already covered by later canonical architecture: player generation requests are bounded, chunk selection advances in concentric rings, population concurrency is configurable, and this fork applies that budget globally.
- Upstream issue #5821 was reviewed and deferred: raising the current recursive leaf-to-log search from four to seven blocks would multiply random-tick traversal cost, while the durable Java-style distance-state design needs block-state, neighbour-update, API-compatibility, and benchmark work.
- Upstream issue #6926 was triaged and implemented: vanilla-command integer parsers now compare and clamp numeric strings before converting them to integers, avoiding PHP 8.5 out-of-range float-string warnings while preserving decimal and exponent input behaviour.
- Upstream pull request #5805 was reviewed and deferred: its incomplete compression-worker branch is 1,562 commits behind stable, has no unit tests or idle-pool cleanup, depends on a queue design with a known ext-pmmpthread degradation case, and can regress login compression onto the main thread when disabled.
- Upstream issue #2731 was reviewed and deferred: detecting an entity standing on cactus safely requires movement collision-direction tracking; prior expanded-AABB approaches triggered adjacent fire incorrectly and later generalized attempts had significant performance cost.
- The #2626 creative left-click regression passed targeted handler PHPUnit, full PHPUnit, touched-file PHPStan, full PHPStan, syntax, whitespace, CI matrix, code-style, and Docker checks.
- The #4206 replacement-chunk tile rebase passed targeted `WorldTest`, touched-file PHPStan, full PHPUnit, full PHPStan, syntax, JSON, whitespace, CI matrix, code-style, and Docker checks.
- The #6800 inventory-search NBT-cache optimization passed targeted `BaseInventoryTest`, touched-file PHPStan, full PHPUnit, full PHPStan, syntax, CI matrix, code-style, and Docker checks.
- The #6818 timezone auto-detection fix passed targeted `TimezoneTest`, touched-file PHPStan, full PHPUnit, full PHPStan, syntax, JSON, whitespace, CI matrix, code-style, and Docker checks.
- The #3272 async light-population lock fix passed targeted `WorldTest`, touched-file PHPStan, full PHPUnit, full PHPStan, syntax, JSON, whitespace, CI matrix, code-style, and Docker checks. The first pushed run failed only PHP-CS-Fixer native-function import style and follow-up commit `4c941c578` restored green CI.
- The #3974 global chunk-population limiter is complete in commit `05a6694b5`: targeted `WorldManagerTest`/`WorldTest`, full PHPUnit, touched-file and full PHPStan, syntax, code generation, translation validation, generated-file collision, JSON, whitespace, GitHub CI, PHP-CS-Fixer, and Docker checks passed.
- The #5064 Query privacy option passed targeted Query PHPUnit, touched-file PHPStan, full PHPUnit, full PHPStan, code generation, translation validation, generated-file collision, JSON, whitespace, CI matrix, code-style, and Docker checks.
- The #5638 timing instrumentation passed syntax checks, touched-file and full PHPStan, full PHPUnit, code generation, translation validation, generated-file collision, whitespace, CI matrix, code-style, and Docker checks.
- The #6714 projectile-close regression passed targeted and full PHPUnit, touched-file and full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, whitespace, CI matrix, code-style, and Docker checks. The initial implementation run failed only PHP-CS-Fixer import ordering; follow-up commit `9270fb248` restored green CI.
- The #6712 saved-hunger regression passed targeted and full PHPUnit, full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, whitespace, CI matrix, code-style, and Docker checks.
- The #5463 updater regression passed targeted and full PHPUnit, full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, whitespace, CI matrix, code-style, and Docker checks.
- The #5603 fence-family regression passed targeted and full PHPUnit, full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, whitespace, CI matrix, code-style, and Docker checks.
- Full repository PHPUnit, PHPStan, code generation, translation validation, source audit, generated-file collision check, Composer validation, Composer install dry-run, JSON validation, syntax checks, and `git diff --check` are clean after the RakLib cookie follow-up. PHP-CS-Fixer 3.75 dry-run was skipped locally because `php-cs-fixer` is not installed in this workspace.
- Fork CI health is the top active gate; stable CI and Docker are green through #6818 implementation commit `425d075bf`.
- Green CI currently warns that `pmmp/setup-php-action@3.2.0` targets deprecated Node.js 20 and is being forced onto Node.js 24; review an action upgrade before GitHub removes compatibility.
- User-facing links were reviewed: fork actions point to `NhanAZ/PocketMine-MP`; PMMP docs, packages, changelog links, and source attribution remain labeled upstream or ecosystem references.
- Release automation is intentionally conservative: source builds and local phars only; GitHub Releases, Docker publishing, updater metadata, Discord, Crowdin, branch sync, and upstream RestrictedActions-style workflows are disabled until fork-owned destinations are configured.
- Protocol verification on 2026-07-02 found the fork current for Bedrock `1.26.30` / protocol `1001`; `BossEventPacket` now has root PHPUnit fixtures for the 1.26.30 fixed-field wire format.

## Priority

1. Red GitHub Actions on the active fork branch.
2. Confirmed security issue or crash regression.
3. Current Bedrock protocol compatibility.
4. A focused PMMP dependency stabilization or import.
5. A useful canonical upstream fix.
6. One actionable upstream issue or pull request.
7. Release, community, maintenance, or documentation cleanup.

Before source-sensitive work, refresh `.github/maintenance-sources/report.json`.
The fork root remains authoritative, canonical PMMP remains the primary change feed, and peer projects remain evidence only.

## Ready Queue

### 1. Review Remaining Upstream Links

- [x] Classify `pmmp.io`, `github.com/pmmp`, Discord, and PocketMine links as ecosystem, upstream, or fork-owned.
- [x] Keep useful ecosystem links, label upstream support clearly, and use fork links for fork-specific actions.

### 2. Decide Fork Release Automation

- [x] Decide whether the fork needs GitHub Releases, phars, Docker images, updater metadata, or source builds only.
- [x] Remove unused upstream-only workflows.
- [x] Enable publishing only with fork-owned credentials and destinations.
- [x] Add a compact release checklist and rollback note.

### 3. Perform A Real Protocol Update

- [x] Verify the current Bedrock version and protocol number from current sources.
- [x] Refresh source monitoring and record exact evidence commits.
- [ ] Use packet evidence or two independent implementations for high-risk fields.
  - 2026-07-02: PMMP BedrockProtocol `b7863bd60042723b91c3cb87ac37309a4fec1309`, Endstone protocol-docs `f00805e98363c6c3024ffd73a56caa5a723cca3a`, and Cloudburst Protocol `f8295d3258fcb4e5c707d852dac981e964b336aa` agree on Bedrock `1.26.30` / protocol `1001`; PMMP and Cloudburst agree on the v1001 `BossEventPacket` fixed-field layout.
- [ ] Update local protocol/data packages and root integration separately from cleanup.
  - `BedrockProtocol` 58.0.0+bedrock-1.26.30 is now imported as a local path package at canonical branch head `b7863bd60042723b91c3cb87ac37309a4fec1309`; no protocol version bump is available yet. Continue watching for the next BedrockProtocol or BedrockData drift before editing protocol code.
- [ ] Regenerate data with `composer run update-codegen` and review generated diffs.
- [ ] Run PHPStan, PHPUnit, phar build, version check, and client smoke tests.
  - 2026-07-02 checks completed: targeted `BossEventPacketTest`, full `tests/phpunit`, and full PHPStan on PHP 8.2. Phar build, version check, and client smoke test were skipped because no protocol or data package changed.
- [ ] Record packet/API/plugin/world risks and rollback guidance.
  - Current risk: protocol compatibility appears current, but no live client smoke test has been performed in this unit.

### 4. Triage One Upstream Item

- [x] Refresh `open-items.json` and `priority-shortlist.json` with the two backlog tools.
  - 2026-07-02: refreshed 419 open issues and 31 open pull requests from `pmmp/PocketMine-MP`; top shortlist remains 40 items.
- [x] Open one high-scoring original issue or PR.
  - Opened issue #6284, "Tile network NBT serialization needs a TypeConverter context".
  - Opened issue #6711, "No serializer registered for pocketmine\item\Item (-10282) Grass".
  - Opened issue #6580, "LevelDB compaction performance issue with large worlds (especially converted worlds)".
  - Opened issue #6808, "Entities are still saved with Java 1.10 IDs".
  - Opened issue #5632, "Update blocks due to missing blockstate properties produced by converters".
  - Opened issue #6832, "Loaded tiles with positions outside the world crash the server".
  - Opened issue #6750, "Server crashed".
  - Opened issue #6861, "Crash when loading firework from disk with negative flight multiplier".
  - Opened issue #6130, "DedicatedQueryNetworkInterface crashes server if unable to bind to udp/19132".
  - Opened issue #5342, "Colours from item custom names leak into /give output"; also reviewed rejected PR #6732 and canonical follow-up commit `c4fb8832fe99e042801dca60f124b7938c94036f`.
  - Opened issue #4830, "ItemEntity doesn't bounce on slime block"; also reviewed abandoned PRs #5095 and #6900, including maintainer review comments.
  - Opened issue #5603, "Incorrect bounding box for Nether Brick Fence and other adjacent fences", including the upstream maintainer's confirmed inheritance root cause.
  - Opened issue #5463, "Update Checker incorrectly detects the server build", including the upstream maintainer's confirmed `IS_DEVELOPMENT_BUILD` versus `BUILD_CHANNEL` root cause.
  - Opened issue #7035, "Protocol 944 LevelDB unsupported 'world'", including both public comments; neither supplied an affected world, level metadata, or chunk-format evidence.
  - Opened issue #6712, "Bad saved hunger values crash the server", filed by dktapps with a crashdump that is no longer publicly retrievable; the stated requirement is graceful rejection of invalid saved values.
  - Opened issue #6714, "Crash when plugins close() projectiles during ProjectileHit(Block|Entity)Event", filed by dktapps; the linked crashdump requires administrator login, but the public issue identifies direct `close()` during projectile-hit events as the crash trigger.
  - Opened issue #5385, "You can no longer interact with the world after rejoining while being in the death screen", and related issue #1567. The discussion identifies client-side `PlayerAuthInputPacket` silence after rejoining dead, plus the broader problem that important death cleanup still happens during respawn.
  - Opened issue #5638, "PlayerAuthInputPacket: Add timings for transaction handling, movement processing etc", filed by dktapps. The issue identifies `PlayerAuthInputPacket` as a high-load packet that mixes movement, state changes, item interactions, and block actions without enough internal timing visibility.
  - Opened issue #5064, "Add a configuration option to disable listing players in GS4 query responses", filed by dktapps. The issue identifies privacy concerns from public GS4 long-query player-name lists while retaining Query for server-list sites.
  - Opened issue #2626, "PlayerInteractEvent::LEFT_CLICK_BLOCK is not fired in creative", filed by dktapps. The issue identifies creative-mode clients skipping repeated start-break packets, which forces plugins such as world-editing tools to combine `PlayerInteractEvent` and `BlockBreakEvent` handling.
  - Opened issue #6781, "Large, long-lived arrays significantly impact cycle GC performance", filed by dktapps. The issue identifies runtime GC scanning cost around large long-lived arrays such as registries, but the proposed direction still needs a safe collection/runtime design.
  - Opened issue #4206, "Tiles may get saved with mismatched coordinates if a chunk is set in a different position than its original", filed by dktapps; also reviewed related blocked issue #6147, "Positionless tiles".
  - Opened issue #6800, "`Item::equals()` and `Item::canStackWith()` rebuilding NBT inside loops during inventory search operations", filed by dktapps. The issue identifies repeated rebuilding of stable search-item NBT in `BaseInventory` loops.
  - Opened issue #6818, "Timezone is not auto-detected when not specified in php.ini", filed by dktapps. The issue identifies PHP 8.2+ reporting `UTC` for unset or invalid `date.timezone`, which prevents PMMP's own timezone auto-detection from running on Windows and Linux builds.
  - Opened issue #3272, "Chunks may be modified while LightPopulationTask is running", filed by dktapps. The issue identifies async light-population results overwriting main-thread lighting changes when terrain changes while the task is still running.
  - Opened issue #3974, "Generator rate limits are ineffective when many parallel worlds are used", filed by dktapps. The issue identifies per-world generation queue limits allowing many loaded worlds to collectively overload the async worker pool.
  - Opened issue #5410, "PM's and Vanilla's flat generators are generating terrain at different heights". The latest maintainer analysis points to the `FlatWorldLayers` string tag in Bedrock `level.dat`, while Mojang creator-tools confirms its current JSON schema and `version.post_1_18` marker.
  - Opened issue #2549, "Generator performance is artificially limited", filed by dktapps. Its proposed direction is to avoid skipping outward through ungenerated terrain and advance chunk sending with generation progress instead of relying on a small hard per-world cap.
  - Opened issue #5821, "Leaves at the edges of large tree canopies generated by bonemeal decay spontaneously", filed by dktapps. The issue confirms that acacia canopy edges can exceed Bedrock's four-block leaf-to-log distance and describes the performance cost of increasing the current traversal radius.
  - Opened issue #6926, "The float-string `300000000000000000000` is not representable as an int, cast occurred", filed by dktapps. The linked crash dump is administrator-only, but the public title and issue text identify unbounded command numeric-string conversion as the failure path.
  - Opened pull request #5805, "First look at specialized network compression threads", by dktapps, including all public comments, related issues #5641 and #1161, and ext-pmmpthread issue #42. The PR remains open with `Status: Incomplete`; its last branch commit is from 2023-11-16.
  - Opened issue #2731, "No damage when standing on top of a cactus", filed by dktapps, including all comments, related magma issue #2041, abandoned PR #4243, and later generalized collision PR #6347. Vanilla Bedrock fixed the original behaviour, but PMMP still lacks a surface-contact callback distinct from `onEntityInside()`.
- [x] Classify relevance and create only the smallest actionable fork task or port.
  - Classified as actionable network/protocol architecture work and implemented the smallest local change: pass `TypeConverter` through tile spawn NBT serialization and cache serialized spawn compounds per converter.
  - Classified #6711 as actionable core validation work and implemented the smallest local guard: `BaseInventory` rejects non-null plain `Item` objects whose type ID maps to a block type ID unless they are real `ItemBlock` instances.
  - Deferred #6580 because the least destructive upstream direction is splitting worlds into region DBs while keeping Mojang keys, which needs an explicit format design, converter path, rollback plan, and large-world benchmarks.
  - Classified #6808 as actionable core save-format work and implemented the smallest local change: make Bedrock entity IDs the default save IDs while preserving legacy aliases for deserialization.
  - Classified #5632 as actionable world-load tolerance work and implemented the smallest local change: registered blockstate deserializers now supply default property values for missing states while preserving failures for unknown IDs, unread properties, invalid values, and wrong tag types.
  - Classified #6832 as actionable crash/corruption handling work and implemented the smallest local change: invalid loaded tiles are logged and skipped before the chunk block-state post-processing path can read out-of-bounds coordinates.
  - Deferred #6750 because the public issue is unconfirmed and the actionable crash dumps are private to upstream maintainers.
  - Classified #6861 as actionable saved-data crash handling and implemented the smallest local change: validate firework rocket `Flight` during item NBT deserialization and convert invalid values to `SavedDataLoadingException`.
  - Classified #6130 as actionable network startup crash handling and implemented the smallest local change: dedicated Query bind failures now satisfy the network-interface exception contract and are handled by the server's localized startup-failure path.
  - Classified #5342 as actionable UI and translation handling work. Adapted the canonical base-format design to the current client-translation parameter-count API instead of using rejected PR #6732's sender-only patch. The plugin-facing change is additive: existing constructor and `translateString()` calls remain valid because the new parameters are optional.
  - Classified #4830 as actionable entity physics work. Moved the block landing hook to `Entity`, retained damage and sounds in `Living`, limited slime bounce to `Living` and `ItemEntity`, and fixed sneaking slime landings to reset fall distance without bouncing.
  - Classified #5603 as actionable block-collision work. Replaced the asymmetric `instanceof static` fence rule with an overridable material-family hook: nether-brick fences connect only to their own fence class and wooden fences connect to every `WoodenFence` variant.
  - Classified #5463 as actionable updater correctness work. Channel suggestions now derive from `VersionInfo::BUILD_CHANNEL`, preserve broader prerelease/development preferences, and name the actual prerelease channel instead of always calling it Beta.
  - Deferred #7035 because protocol compatibility and world-format compatibility are intentionally independent. The current network protocol is `1001`, but `WorldDataVersions::NETWORK` remains `924`; blindly raising it to `944` or the suggested `975` would bypass the guard without proving support for newer chunks, subchunks, blockstates, or world metadata.
  - Classified #6712 as actionable saved-data crash handling. `Human` now applies all four hunger fields through one guarded loader and preserves strict `HungerManager` ranges while translating invalid values to the repository's standard `SavedDataLoadingException` contract.
  - Classified #6714 as actionable plugin API crash handling. `Projectile::move()` now returns immediately when a hit event, generic hit hook, or hit-target hook closes the projectile, avoiding post-close hit effects, collision state changes, world movement indexing, and block-intersection checks.
  - Deferred #5385 because the current forced respawn happens after the pre-spawn sequence and `PlayerJoinEvent`, while a correct fix needs the respawn target, health reset, attributes, and `PlayerRespawnEvent` before the initial client state is sent. Moving `PlayerRespawnEvent` into login could break plugins that send messages, forms, or other in-game-only effects during respawn.
  - Classified #5638 as actionable performance observability work. `Timings` now exposes a `PlayerAuthInputPacket` group with child timers for input flags, movement, item-use transactions, item-stack requests, and block actions, and `InGamePacketHandler` records those sections without changing packet behaviour.
  - Classified #5064 as actionable Query privacy work. `QueryInfo` now keeps real player counts and server-side player-list data, but hides names from GS4 long-query output when `settings.query-player-list` is false; plugins can still override the flag through `QueryRegenerateEvent`.
  - Classified #2626 as actionable plugin API compatibility work. `InGamePacketHandler` now calls `attackBlock()` during creative `PREDICT_DESTROY_BLOCK` only when the same block was not already attacked by `START_BREAK` or `CONTINUE_DESTROY_BLOCK`, restoring `PlayerInteractEvent::LEFT_CLICK_BLOCK` without duplicating normal left-click events.
  - Deferred #6781 because a correct fix needs a runtime-supported non-collectable array, static-array refactor, or explicit registry collection abstraction with benchmarks; guessing at wrappers across registries would risk performance regressions and hidden memory/lifetime bugs.
  - Classified #4206 as actionable chunk replacement correctness work. `World::setChunk()` now rebases tile coordinates already present in the replacement chunk, keeps shared `Position` holders stable for block inventories, clears spawnable tile caches, and shifts chest pair coordinates with the moved tile.
  - Classified #6800 as actionable inventory performance work. `BaseInventory` now prepares search-item tags once per inventory scan and passes them to a cached matching helper; the default helper preserves subclass overrides of `getMatchingItemCount()`, while `SimpleInventory` and `DoubleChestInventory` use the fast path.
  - Classified #6818 as actionable startup/UI correctness work. `Timezone::init()` now treats `UTC` from `date.timezone` as a fallback value that should continue into PMMP's system and geolocation auto-detection paths, while explicit timezone identifiers and abbreviations remain supported.
  - Classified #3272 as actionable world-lighting correctness work. `orderLightPopulation()` now locks and keeps loaded the target chunk while async light calculation runs, and `completeLightPopulation()` applies results only if the same lock survives until completion.
  - Classified #3974 as actionable world-generation throttling work. `WorldManager` now owns a server-wide population task slot limiter, while each `World` still keeps its existing per-world limiter and wakes queued population requests across loaded worlds when a global slot is released.
  - Deferred #5410 because adding `FlatWorldLayers` alone would not align PMMP's current Y=0 flat generation with imported post-1.18 worlds. Revisit with representative pre-1.18 and post-1.18 Bedrock worlds, a PMMP-created flat world, confirmed offset/version semantics, round-trip tests, and a live Vanilla client generation smoke test.
  - Classified #2549 as already covered by later canonical work: commit `5107d0df4e71d9cd5dd11516c6c0faf2186dcb52` caps active player generation requests, commit `dbf9a331600b1e0daa90d2ee05d54415b2939e22` orders chunks in concentric rings, and `population-queue-size` is configurable. Added ring-order/completeness regression coverage and clarified that the fork's population budget now applies across all worlds.
  - Deferred #5821 because the stable and major-next branches still use the same bounded recursive search, and merely raising `Leaves::MAX_LOG_DISTANCE` would expand work on a random-tick hot path. Revisit with a Java-style cached distance state, bounded neighbour propagation, generated-tree coverage, traversal benchmarks, and an explicit block-state/plugin compatibility plan.
  - Classified #6926 as actionable command-input hardening. `getInteger()` and `getBoundedInt()` now convert numeric strings to floats for bounds checks and only cast after the result is proven safe, preserving existing truncation, decimal, exponent, clamp, and localized error behaviour without exposing PHP 8.5's out-of-range cast warning.
  - Deferred #5805 because directly porting its 341-line experimental thread-pool change would replace the current forced async login path without resolving disabled-thread behaviour, worker-pool lifetime, queue degradation, shutdown/error propagation, chunk compression, or modern branch integration. Revisit only with current profiling on Windows and Linux, a non-disableable or legacy fallback for login compression, bounded queue/backpressure and idle cleanup, deterministic shutdown/crash handling, and PHPUnit plus load benchmarks.
  - Deferred #2731 because `onEntityInside()` intentionally handles cell intersection, not collision-surface contact. Expanding the scanned AABB caused adjacent fire false positives in PR #4243, while PR #6347's generalized adjacent scan was abandoned after measurable collision-performance impact. Revisit by recording effective movement collision directions, checking only the contacted faces and blocks with a dedicated surface-collision action, reworking player ground-state integration where required, and testing cactus, magma, fire adjacency, slabs, carpets, walls, stationary contact, and movement cost.
- [x] Preserve source links and authorship; do not copy whole discussions.
  - Sources: https://github.com/pmmp/PocketMine-MP/issues/6284, https://github.com/pmmp/PocketMine-MP/issues/6711, https://github.com/pmmp/PocketMine-MP/issues/6580, https://github.com/pmmp/PocketMine-MP/issues/6808 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5632 by GH-PM with a dktapps root-cause comment, https://github.com/pmmp/PocketMine-MP/issues/6832 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6750 by LeonMazzoli with private upstream crash dumps, https://github.com/pmmp/PocketMine-MP/issues/6861 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6130 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5342 plus canonical commit https://github.com/pmmp/PocketMine-MP/commit/c4fb8832fe99e042801dca60f124b7938c94036f by dktapps, https://github.com/pmmp/PocketMine-MP/issues/4830 by NTT1906 with abandoned implementation evidence from PR #5095 by Swift-Strider and PR #6900 by siyFred, https://github.com/pmmp/PocketMine-MP/issues/5603 by DaisukeDaisuke with the inheritance root cause confirmed by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5463 by Gaprix with the build-channel root cause confirmed by dktapps, https://github.com/pmmp/PocketMine-MP/issues/7035 by HungerWars951, https://github.com/pmmp/PocketMine-MP/issues/6712 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6714 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5385 by JavierLeon9966, related https://github.com/pmmp/PocketMine-MP/issues/1567 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5638 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5064 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/2626 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6781 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/4206 by dktapps, related https://github.com/pmmp/PocketMine-MP/issues/6147 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6800 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6818 by dktapps with PHP 8.2 datetime configuration evidence, https://github.com/pmmp/PocketMine-MP/issues/3272 by dktapps with the chunk-locking direction confirmed in the issue comments, https://github.com/pmmp/PocketMine-MP/issues/3974 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/5410 by ipad54 with Mojang creator-tools commit https://github.com/Mojang/minecraft-creator-tools/commit/18988d351bca16019c664e320e34a246fe9347ec plus PowerNukkitX and Dragonfly tag-shape corroboration, https://github.com/pmmp/PocketMine-MP/issues/2549 by dktapps with canonical commits https://github.com/pmmp/PocketMine-MP/commit/5107d0df4e71d9cd5dd11516c6c0faf2186dcb52 and https://github.com/pmmp/PocketMine-MP/commit/dbf9a331600b1e0daa90d2ee05d54415b2939e22, https://github.com/pmmp/PocketMine-MP/issues/5821 by dktapps, https://github.com/pmmp/PocketMine-MP/issues/6926 by dktapps with PHP conversion behaviour confirmed in https://github.com/php/php-src/blob/master/Zend/zend_operators.c, https://github.com/pmmp/PocketMine-MP/pull/5805 by dktapps with related https://github.com/pmmp/PocketMine-MP/issues/5641, https://github.com/pmmp/PocketMine-MP/issues/1161, and https://github.com/pmmp/ext-pmmpthread/issues/42, and https://github.com/pmmp/PocketMine-MP/issues/2731 by dktapps with related https://github.com/pmmp/PocketMine-MP/issues/2041 plus abandoned PRs https://github.com/pmmp/PocketMine-MP/pull/4243 and https://github.com/pmmp/PocketMine-MP/pull/6347; original discussions and review comments were read before implementation or deferral.
  - Checks: targeted `SpawnableTest`, targeted `BaseInventoryTest`, targeted `EntityFactoryTest`, targeted `BlockSerializerDeserializerTest`, targeted `WorldTest`, targeted `ItemTest`, full PHPUnit, full PHPStan, PHP-CS-Fixer 3.75 dry-run, JSON validation, translation validation, `git diff --check`, and generated-file collision check. For #4830 specifically, item, XP-orb, sneaking, sound, and living bounce regressions passed along with full PHPUnit (227 tests, 72,572 assertions), full PHPStan, and PHP-CS-Fixer 3.75. A live client gameplay smoke test remains pending.
  - The candidates recorded above have been reviewed. Refresh and read original items before adding another; the dependency import queue is complete for the current PMMP-owned package list.
  - 2026-07-03: refreshed 419 open issues and 31 open pull requests, then implemented the first unreviewed actionable candidate, #5603. Targeted coverage passed (1 test, 5 assertions), full PHPUnit passed (233 tests, 72,606 assertions), and full PHPStan plus repository consistency gates passed.
  - 2026-07-03: implemented #5463 with focused channel-resolution coverage (1 test, 7 assertions), full PHPUnit (234 tests, 72,613 assertions), full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, and whitespace checks.
  - 2026-07-03: deferred #7035 after confirming canonical commit `15bdc9a2fa99db267d2d2a5523d0fa0dd4dde5fa` deliberately capped supported worlds at NetworkVersion `924` for Bedrock 1.26.0. Revisit only with an affected world plus canonical schema/format support or equivalent reproducible evidence.
  - 2026-07-03: implemented #6712 with invalid food, exhaustion, saturation, and tick-timer coverage plus a valid-data control (5 tests, 12 assertions), full PHPUnit (239 tests, 72,625 assertions), full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, and whitespace checks.
  - 2026-07-03: implemented #6714 with block-hit and entity-hit event-close coverage (2 tests, 8 assertions), full PHPUnit (241 tests, 72,633 assertions), full PHPStan, code generation, translation validation, generated-file collision, JSON, syntax, whitespace, CI matrix, code-style, and Docker checks. The first pushed run failed PHP-CS-Fixer import ordering and was corrected in follow-up commit `9270fb248`.
  - 2026-07-03: reviewed #5385 and related #1567, then deferred the fix because the safe direction is a respawn/login lifecycle refactor rather than a small compatibility-neutral patch. Source audit still reports 19 sources with eight review signals.
  - 2026-07-03: implemented #5638 with `PlayerAuthInputPacket` section timings. Syntax checks, touched-file PHPStan, full PHPStan, full PHPUnit (241 tests, 72,633 assertions), code generation, translation validation, generated-file collision, whitespace, CI matrix, code-style, and Docker checks passed.
  - 2026-07-03: implemented #5064 with `settings.query-player-list`, QueryInfo cache invalidation coverage, and hidden-name long-query coverage. Targeted Query PHPUnit (4 tests, 18 assertions), touched-file PHPStan, full PHPUnit (244 tests, 72,647 assertions), full PHPStan, code generation, translation validation, generated-file collision, JSON, whitespace, CI matrix, code-style, and Docker checks passed. Source audit now records PowerNukkitX `0d0a3b4f9362f4188d3e2b65df47bc93b4f41975` as reference-only evidence for future map-image triage.
  - 2026-07-03: implemented #2626 with handler-level creative/survival destroy-action coverage (3 tests, 10 assertions), full PHPUnit (247 tests, 72,657 assertions), touched-file and full PHPStan, syntax, whitespace, CI matrix, code-style, and Docker checks. CI still reports only the known non-blocking `pmmp/setup-php-action@3.2.0` Node.js 20 deprecation warning.
  - 2026-07-03: refreshed the upstream backlog to 419 open issues and 31 open pull requests, reviewed #6781 and deferred it as too broad for a small safe patch, then implemented #4206 with replacement-chunk tile rebase coverage. Targeted `WorldTest` (2 tests, 9 assertions), full PHPUnit (248 tests, 72,663 assertions), touched-file PHPStan, full PHPStan, syntax, JSON, whitespace, CI matrix, code-style, and Docker checks passed. CI still reports only the known non-blocking `pmmp/setup-php-action@3.2.0` Node.js 20 deprecation warning.
  - 2026-07-03: refreshed the upstream backlog to 419 open issues and 31 open pull requests, then implemented #6800. Targeted `BaseInventoryTest` (11 tests, 28 assertions), full PHPUnit (250 tests, 72,667 assertions), touched-file PHPStan, full PHPStan, syntax, CI matrix, code-style, and Docker checks passed. CI still reports only the known non-blocking `pmmp/setup-php-action@3.2.0` Node.js 20 deprecation warning.
  - 2026-07-03: refreshed the upstream backlog to 419 open issues and 31 open pull requests, then implemented #6818. Targeted `TimezoneTest` (2 tests, 7 assertions), full PHPUnit (252 tests, 72,674 assertions), touched-file PHPStan, full PHPStan, syntax, JSON, whitespace, CI matrix, code-style, and Docker checks passed.
  - 2026-07-03: refreshed source audit and the upstream backlog to 419 open issues and 31 open pull requests, then implemented #3272. Targeted `WorldTest` (4 tests, 22 assertions), full PHPUnit (254 tests, 72,687 assertions), touched-file PHPStan, full PHPStan, syntax, JSON, whitespace, CI matrix, code-style, and Docker checks passed. The first pushed run failed only PHP-CS-Fixer native-function import style and follow-up commit `4c941c578` restored green CI.
  - 2026-07-03: refreshed source audit and the upstream backlog to 419 open issues and 31 open pull requests, then implemented #3974 in commit `05a6694b5`. Targeted `WorldManagerTest`/`WorldTest` (6 tests, 33 assertions), full PHPUnit (256 tests, 72,698 assertions), touched-file and full PHPStan, syntax, code generation, translation validation, generated-file collision, JSON, whitespace, GitHub CI, PHP-CS-Fixer, and Docker checks passed.
  - 2026-07-03: refreshed source audit and the upstream backlog to 419 open issues and 31 open pull requests, then reviewed #5410. The issue, Mojang creator-tools, PowerNukkitX, and Dragonfly confirm the `FlatWorldLayers` string tag, but no safe code change was made without fixtures and offset/version round-trip evidence.
  - 2026-07-03: reviewed #2549 as already covered by canonical player request throttling and concentric chunk selection plus this fork's global population limiter. Added `ChunkSelectorTest` regression coverage and corrected the `population-queue-size` description in commit `2c705b83f`. Targeted PHPUnit (1 test, 1,569 assertions), full PHPUnit (257 tests, 74,267 assertions), touched-file and full PHPStan, syntax, code generation, translation validation, generated-file collision, JSON, whitespace, GitHub CI, PHP-CS-Fixer, and Docker checks passed.
  - 2026-07-03: reviewed #5821 against `stable`, `major-next`, the original acacia-tree commit, and the issue's three proposed directions. Deferred implementation until a cached distance-state design can avoid multiplying recursive random-tick work and can be covered by tree-generation tests and benchmarks. JSON and whitespace validation passed; GitHub CI and Docker passed for decision commit `e8cc27251`.
  - 2026-07-03: implemented #6926 with pre-cast bounds handling and four regression tests covering extreme positive/negative strings, localized rejection, decimal/exponent compatibility, and invalid syntax. Targeted PHPUnit (4 tests, 8 assertions), full PHPUnit (261 tests, 74,275 assertions), touched-file and full PHPStan, syntax, code generation, translation validation, generated-file collision, JSON, whitespace, PHP 8.1-8.5 CI, PHP-CS-Fixer, integration, and Docker checks passed for commit `5fddf6251`.
  - 2026-07-03: reviewed #5805 and deferred its specialized compression-worker pool. The open incomplete branch is 1,562 commits behind stable, has no tests, retains unresolved queue and idle-pool concerns, and may move forced login compression back onto the main thread when workers are disabled. Source audit, review JSON, whitespace, GitHub CI, PHP-CS-Fixer, and Docker checks passed for decision commit `32ad9a0c1`; no runtime-specific tests were required because source code did not change.
  - 2026-07-03: reviewed #2731 and deferred cactus top-contact damage until movement collision directions can drive a dedicated surface-contact action without broad adjacent-block scans. Prior attempts produced fire false positives or unacceptable AABB scan cost; source audit, review JSON, and whitespace validation are the only required local checks because no runtime source changed.
  - Next ready step: read upstream issue #4649, "Tree generation is not consistent", and classify the smallest safe fork task before adding implementation work here.

## Dependency Track

Completed: `bedrock-block-upgrade-schema`, `bedrock-data`, `bedrock-item-upgrade-schema`, `bedrock-protocol`, `binaryutils`, `callback-validator`, `color`, `errorhandler`, `log`, `math`, `nbt`, `raklib`, `raklib-ipc`, `snooze`.

`callback-validator` was imported from tag `1.0.4` / canonical head `143fa6e13254f1ab90c31b223982016f95635c37` with its MIT license, subtree history, package tests, and continued canonical source monitoring. Its 104 package tests / 499 assertions and package PHPStan passed; root PHPUnit remained at 227 tests / 72,572 assertions and all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`binaryutils` was imported from tag `0.2.7` / canonical stable head `14c044afa33cb581b4a6d1ea04a87e0bc99e824b` with its LGPL-3.0 license, subtree history, package tests, and continued canonical source monitoring. Its four package tests / four assertions and package PHPStan passed; root PHPUnit remained at 227 tests / 72,572 assertions and all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`nbt` was imported from tag `1.2.0` / root lock pin `51b8d6a97065fb93e0b4f660b65164b6e1ed2fff` with its LGPL-3.0 license, subtree history, package tests, and continued canonical source monitoring. The later stable commit `5429a21c9321eb154f7fcdaa789d2b5ccbcc6ea3` only updates PHP 8.5 CI and PHPStan, so it remains deferred. Its 75 package tests / 151 assertions and package PHPStan passed; root PHPUnit remained at 227 tests / 72,572 assertions and all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`bedrock-data` was imported from tag `6.7.0+bedrock-1.26.30` / canonical master head `bdb44a48fb6beffb6e9f6864f06d2232eb62b6a3` with its CC0-1.0 license, subtree history, package metadata, and continued canonical source monitoring. It has no package test suite; package Composer validation passed, and root PHPUnit remained at 227 tests / 72,572 assertions with all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`bedrock-item-upgrade-schema` was imported from tag `1.17.0+bedrock-1.26.20` / canonical master head `e19685d2e7e76eb7446115c556df34e5d627d072` with its CC0-1.0 license, subtree history, package metadata, and continued canonical source monitoring. It has no package test suite; package Composer validation passed, and root PHPUnit remained at 227 tests / 72,572 assertions with all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`bedrock-block-upgrade-schema` was imported from tag `5.2.0+bedrock-1.21.110` / root lock pin `5d7889c9a1cdf9e3cd814d2a104ad69b75116ec7` with its CC0-1.0 license, subtree history, package metadata, schema files, and continued canonical source monitoring. The later master commit `581e29ffecfc846e982203d473f5901e995f7165` only updates `actions/setup-node`, so it remains deferred. It has no package test suite; package Composer validation passed, and root PHPUnit remained at 227 tests / 72,572 assertions with all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`bedrock-protocol` was imported from tag `58.0.0+bedrock-1.26.30` / canonical master head `b7863bd60042723b91c3cb87ac37309a4fec1309` with its LGPL-3.0 license, subtree history, package metadata, protocol sources, tests, tools, and continued canonical source monitoring. This is a local ownership import of the already-installed protocol package, not a Bedrock wire-format bump. Its package PHPUnit suite and package PHPStan passed; the package create-method generator was executed and reviewed, but its fully-qualified type-hint churn was left uncommitted to preserve the canonical tag source. Root PHPUnit remained at 227 tests / 72,572 assertions with all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`snooze` was imported from tag `0.5.0` / root lock pin `a86d9ee60ce44755d166d3c7ba4b8b8be8360915` with its LGPL-3.0 license, subtree history, package metadata, static-analysis config, and thread-notification sources. The later master commits through `fd249c13569bb7b7d564f331684cdfaeba24d388` only update CI, PHPStan config, and one PHPStan-only inline `@var` comment in `SleeperHandler`, so they remain deferred. It has no package PHPUnit suite; package PHPStan passed, and root PHPUnit remained at 227 tests / 72,572 assertions with all root gates passed. Composer validation only reports the pre-existing deprecated `LGPL-3.0` SPDX identifier warning.

`raklib` was imported from tag `1.2.1` / root lock pin `669eb4d1e644f91437323ef24ce3ee985182b829` with its GPL-3.0 license, subtree history, package metadata, protocol/server sources, tools, and static-analysis config. Canonical stable commit `7655018631147f0b5d473326fddc2faea105dffe` was adapted as a focused follow-up: offline handshake cookies are supported, cookie mismatches are counted between rotations, PMMP exposes `network.raklib-cookie-rotation-interval`, and the setting defaults to `0` so legacy/OVH-sensitive deployments remain unchanged unless explicitly enabled. It has no package PHPUnit suite; package PHPStan, root RakLib cookie tests, full root PHPUnit, and root PHPStan passed. Composer validation only reports deprecated SPDX identifier warnings for `GPL-3.0` in RakLib and the pre-existing root `LGPL-3.0`.

`raklib-ipc` was imported from tag `1.0.1` / root lock pin `ce632ef2c6743e71eddb5dc329c49af6555f90bc` with its GPL-3.0 license, subtree history, package metadata, IPC channel sources, and static-analysis config. The PHPStan-only type assertion from later master commit `dd79c4344640ab0d086f08ad47fe83f52b7d3371` was adapted so package and root static analysis stay clean; the remaining post-tag commits only update CI and dev PHPStan dependencies, so they remain deferred. It has no package PHPUnit suite; package PHPStan passed, and root PHPUnit remained at 227 tests / 72,572 assertions with all root gates passed. Composer validation only reports deprecated SPDX identifier warnings for `GPL-3.0` in RakLib IPC and the pre-existing root `LGPL-3.0`.

The dependency import queue is complete for the current PMMP-owned package list.

Import one package at a time. Preserve its license, source layout, Composer metadata, pin, and useful history.
Local ownership does not stop monitoring its canonical PMMP repository.
Continue monitoring future RakLib tags and network-behaviour commits through source audit before importing more.
Next ready order after this dependency pass: refresh the upstream backlog and triage one actionable item, unless Actions turn red or a protocol/security issue appears first.

## Milestones

- **Foundation hardening: active.** Changelog habit is established; keep `changelogs/fork.md` current for each completed unit.
- **Self-contained dependencies: active.** Fourteen packages are local; the first dependency import pass is complete.
- **Protocol velocity: active.** Evidence rules are ready; one real verified protocol update is still required.
- **Fork releases: planned.** Automated publishing is disabled; release naming and artifact policy remain before public releases.
- **Community throughput: planned.** Keep intake neutral, focused, reproducible, and fast without mass-importing upstream noise.

## Maintenance Commands

```powershell
$env:GITHUB_TOKEN = gh auth token
php tools/audit-maintenance-sources.php
php tools/fetch-upstream-backlog.php
php tools/prioritize-upstream-backlog.php
Remove-Item Env:GITHUB_TOKEN
```

Generated maintenance data belongs under `.github/maintenance-sources/` and `.github/upstream-intake/` as JSON.

## Continuation

When asked to continue this roadmap:

1. Read `AGENTS.md` and this file only, then inspect code relevant to the first ready task.
2. Refresh external data only when the task depends on it.
3. Continue through review-only deferrals until one actionable code unit is completed with proportional checks, unless a higher-priority gate intervenes or no ready code candidate remains.
4. If a push touches workflow-covered code, run the closest local checks before pushing.
5. Update this file live, including discoveries and deferrals.
6. Commit and push when requested.
