name: Update updater API info

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this personal fork. The upstream update.pmmp.io deployment key is not configured here."
