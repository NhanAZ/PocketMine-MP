name: Update Docker images

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this personal fork. Configure fork-owned container registry credentials before enabling image publishing."
