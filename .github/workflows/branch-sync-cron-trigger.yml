name: Trigger branch sync

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this personal fork. Configure an explicit upstream sync workflow before enabling scheduled branch sync."
