name: Trigger Crowdin download

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this personal fork. Upstream Crowdin RestrictedActions automation is not configured here."
