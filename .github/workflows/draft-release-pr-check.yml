name: Release PR checks

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this personal fork. Automated release PR checks depend on the upstream release workflow and will be rebuilt when fork release publishing is enabled."
