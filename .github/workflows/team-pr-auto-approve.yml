name: Auto approve collaborator PRs

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this personal fork. Upstream RestrictedActions automation is not configured here."
